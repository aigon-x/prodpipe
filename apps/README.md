# apps

> Katalog aplikacji AIGON Production Platform — definicje aplikacji, usługi, frontendy.

## 1. Purpose
Przechowuje definicje aplikacji: usługi, frontendy, komponenty aplikacyjne.

## 2. Owner
`STATUS: FOUNDATION PLACEHOLDER`

## 3. Source of Truth
Git jest źródłem prawdy (desired state) dla definicji aplikacji. Runtime odzwierciedla actual state.

## 4. Contains
Definicje aplikacji, usługi, frontendy, komponenty aplikacyjne.

## 5. Does Not Contain
Nie zawiera sekretów ani runtime state.

## 6. Dependencies
Zależy od system, config, deployment.

## 7. Consumers
Operatorzy, deweloperzy, użytkownicy.

## 8. Synchronization
Klasa: `CANONICAL` — definicje aplikacji są źródłem prawdy w git.

## 9. Lifecycle
`STATUS: FOUNDATION PLACEHOLDER`

## 10. Security
Klasyfikacja danych: SYSTEM. Brak sekretów w git — tylko definicje i referencje.

## 11. Recovery
`STATUS: FOUNDATION PLACEHOLDER` — procedury odzyskiwania definicji aplikacji.

## 12. Drift Detection
Wykrywanie rozjazdu między definicjami aplikacji (git) a faktycznym stanem (Runtime). `STATUS: FOUNDATION PLACEHOLDER`.

## Examples
`STATUS: FOUNDATION PLACEHOLDER`
