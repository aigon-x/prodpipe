name: Drift

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]
  schedule:
    - cron: '17 3 * * *'   # codziennie 03:17 UTC

jobs:
  drift:
    name: Drift detection (Git desired vs Runtime actual)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — drift reconciliation
        run: echo "Drift: brak implementacji (reconciliation engine — Git desired vs Runtime actual → PASS/DRIFT)"
