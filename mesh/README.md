# mesh

> Katalog mesh AIGON Production Platform — definicje mesh, topologia, węzły, komunikacja.

## 1. Purpose
Przechowuje definicje mesh: topologia, węzły, komunikacja, synchronizacja między węzłami.

## 2. Owner
`STATUS: FOUNDATION PLACEHOLDER`

## 3. Source of Truth
Git jest źródłem prawdy (desired state) dla definicji mesh. Runtime odzwierciedla actual state.

## 4. Contains
Definicje mesh, topologia, węzły, komunikacja, synchronizacja.

## 5. Does Not Contain
Nie zawiera sekretów ani runtime state.

## 6. Dependencies
Zależy od system, config, contracts.

## 7. Consumers
Runtime, węzły mesh, operatorzy.

## 8. Synchronization
Klasa: `CANONICAL` — definicje mesh są źródłem prawdy w git.

## 9. Lifecycle
`STATUS: FOUNDATION PLACEHOLDER`

## 10. Security
Klasyfikacja danych: SYSTEM. Brak sekretów w git — tylko definicje i referencje.

## 11. Recovery
`STATUS: FOUNDATION PLACEHOLDER` — procedury odzyskiwania definicji mesh.

## 12. Drift Detection
Wykrywanie rozjazdu między definicjami mesh (git) a faktycznym stanem (Runtime). `STATUS: FOUNDATION PLACEHOLDER`.

## Examples
`STATUS: FOUNDATION PLACEHOLDER`
