# tools

> Katalog narzędzi AIGON Production Platform — skrypty, automatyzacja, walidacja, integrity.

## 1. Purpose
Przechowuje narzędzia: skrypty, automatyzację, walidację, integrity checks, secret-scan.

## 2. Owner
`STATUS: FOUNDATION PLACEHOLDER`

## 3. Source of Truth
Git jest źródłem prawdy (desired state) dla narzędzi. Runtime odzwierciedla actual state.

## 4. Contains
Skrypty, automatyzacja, walidacja, integrity checks, secret-scan, narzędzia CI.

## 5. Does Not Contain
Nie zawiera sekretów ani runtime state.

## 6. Dependencies
Zależy od config, governance, deployment.

## 7. Consumers
Operatorzy, CI/CD, narzędzia wdrożeniowe.

## 8. Synchronization
Klasa: `CANONICAL` — narzędzia są źródłem prawdy w git.

## 9. Lifecycle
`STATUS: FOUNDATION PLACEHOLDER`

## 10. Security
Klasyfikacja danych: SYSTEM. Brak sekretów w git — tylko skrypty i referencje.

## 11. Recovery
`STATUS: FOUNDATION PLACEHOLDER` — procedury odzyskiwania narzędzi.

## 12. Drift Detection
Wykrywanie rozjazdu między narzędziami (git) a faktycznym stanem (Runtime). `STATUS: FOUNDATION PLACEHOLDER`.

## Examples
`STATUS: FOUNDATION PLACEHOLDER`
