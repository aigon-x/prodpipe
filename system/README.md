# system

> Katalog systemowy AIGON Production Platform — definicje systemu, topologia, komponenty.

## 1. Purpose
Przechowuje definicje systemu: topologia, komponenty, architektura systemowa.

## 2. Owner
`STATUS: FOUNDATION PLACEHOLDER`

## 3. Source of Truth
Git jest źródłem prawdy (desired state) dla definicji systemu. Runtime odzwierciedla actual state.

## 4. Contains
Definicje systemu, topologia, komponenty, architektura systemowa.

## 5. Does Not Contain
Nie zawiera sekretów ani runtime state.

## 6. Dependencies
Zależy od contracts, config, governance.

## 7. Consumers
Operatorzy, architekci, narzędzia wdrożeniowe.

## 8. Synchronization
Klasa: `CANONICAL` — definicje systemu są źródłem prawdy w git.

## 9. Lifecycle
`STATUS: FOUNDATION PLACEHOLDER`

## 10. Security
Klasyfikacja danych: SYSTEM. Brak sekretów w git — tylko definicje i referencje.

## 11. Recovery
`STATUS: FOUNDATION PLACEHOLDER` — procedury odzyskiwania definicji systemu.

## 12. Drift Detection
Wykrywanie rozjazdu między definicjami systemu (git) a faktycznym stanem (Runtime). `STATUS: FOUNDATION PLACEHOLDER`.

## Examples
`STATUS: FOUNDATION PLACEHOLDER`
