# Config Schema — AIGON Production Platform

> **STATUS: CANONICAL** — zdefiniowano schemat walidacji canonical config.

## Cel
Definicje schematów konfiguracji (JSON Schema / YAML Schema).

## Zakres
- `platform.schema.json` — JSON Schema walidacji `config/canonical/platform.yaml`
- Walidacja konfiguracji przez `tools/config/config-compiler.sh validate`
- Wersjonowanie schematów (`schema_version`)

## Status
**STATUS: CANONICAL** — `platform.schema.json` zdefiniowany i używany przez config-compiler.
