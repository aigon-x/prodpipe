# ============================================================================
# gates.yaml — Definicje gate'ów (metadata-driven). ŹRÓDŁO PRAWDY dla profiles.sh.
# ============================================================================
# Ten plik jest jedynym miejscem, w którym definiuje się moduły/gate'y:
#   - nazwa modułu (module)
#   - ścieżka skryptu (script) — względem tools/verify/
#   - profile, w których moduł działa, oraz severity (BLOCKING | WARNING)
#
# profiles.sh jest GENEROWANY z tego pliku przez tools/verify/core/gen-profiles.sh.
# NIE edytuj profiles.sh ręcznie — edytuj ten plik i uruchom generator.
#
# Poziomy profili (L0-L3):
#   L0 FAST   — pre-commit, <5-10s, oczywiste błędy
#   L1 FULL   — pre-push, <1-3min, pełna certyfikacja lokalnego drzewa
#   L2 REMOTE — CI, niezależna weryfikacja
#   L3 RELEASE/GENESIS — pełna certyfikacja baseline/release
# ============================================================================

schema_version: 1

# ── Moduły / gate'y ─────────────────────────────────────────────────────────
# Każdy gate: nazwa modułu, ścieżka skryptu, profile w których działa + severity.
gates:
  - module: git
    script: git/integrity.sh
    profiles:
      fast: BLOCKING
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security
    script: security/secrets.sh
    profiles:
      fast: BLOCKING
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: structure
    script: structure/readme.sh
    profiles:
      fast: BLOCKING
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: architecture
    script: architecture/architecture.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: dependencies
    script: dependencies/dependencies.sh
    profiles:
      full: WARNING
      release: BLOCKING
      genesis: BLOCKING
  - module: reproducibility
    script: reproducibility/reproducibility.sh
    profiles:
      full: WARNING
      release: BLOCKING
      genesis: BLOCKING
  - module: deployment
    script: deployment/deployment.sh
    profiles:
      full: WARNING
      release: BLOCKING
      genesis: BLOCKING
  - module: contracts
    script: contracts/contracts.sh
    profiles:
      full: WARNING
      release: BLOCKING
      genesis: BLOCKING
  - module: aesthetics
    script: aesthetics/aesthetics.sh
    profiles:
      fast: BLOCKING
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: semantics
    script: architecture/semantics.sh
    profiles:
      full: BLOCKING
      architecture: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security-drills
    script: security/drills.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: taxonomy
    script: architecture/taxonomy.sh
    profiles:
      full: BLOCKING
      architecture: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: i18n
    script: i18n/i18n.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: efficiency
    script: efficiency/efficiency.sh
    profiles:
      full: BLOCKING
      architecture: BLOCKING
  - module: obs-logging
    script: obs-logging-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-metrics
    script: obs-metrics-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-alerts
    script: obs-alerts-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-slo
    script: obs-slo-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-health
    script: obs-health-endpoints.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-deadman
    script: obs-deadman-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-dashboards
    script: obs-dashboards-check.sh
    profiles:
      full: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: obs-synthetics
    script: obs-synthetics-check.sh
    profiles:
      full: BLOCKING
  - module: security-credential-registry
    script: security/sec-credential-registry.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security-rotation
    script: security/sec-rotation-check.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security-retention
    script: security/sec-retention-check.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security-auth
    script: security/sec-auth-check.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING
  - module: security-web
    script: security/sec-web-check.sh
    profiles:
      full: BLOCKING
      security: BLOCKING
      release: BLOCKING
      genesis: BLOCKING

# ── Profile → domyślna lista modułów (kolejność uruchamiania) ───────────────
profiles:
  fast: [git, security, structure, aesthetics]
  full: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, efficiency]
  full: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, obs-logging, obs-metrics, obs-alerts, obs-slo, obs-health, obs-deadman, obs-dashboards, obs-synthetics]
  security: [git, security, security-drills]
  architecture: [architecture, contracts, semantics, taxonomy, efficiency]
  reproducibility: [reproducibility, dependencies]
  release: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, efficiency]
  genesis: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, efficiency]
  all: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, efficiency]
  release: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, obs-logging, obs-metrics, obs-alerts, obs-slo, obs-health, obs-deadman, obs-dashboards, obs-synthetics]
  genesis: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, obs-logging, obs-metrics, obs-alerts, obs-slo, obs-health, obs-deadman, obs-dashboards, obs-synthetics]
  all: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, obs-logging, obs-metrics, obs-alerts, obs-slo, obs-health, obs-deadman, obs-dashboards, obs-synthetics]
  full: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, security-credential-registry, security-rotation, security-retention, security-auth, security-web]
  security: [git, security, security-drills, security-credential-registry, security-rotation, security-retention, security-auth, security-web]
  architecture: [architecture, contracts, semantics, taxonomy]
  reproducibility: [reproducibility, dependencies]
  release: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, security-credential-registry, security-rotation, security-retention, security-auth, security-web]
  genesis: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, security-credential-registry, security-rotation, security-retention, security-auth, security-web]
  all: [git, security, structure, architecture, dependencies, reproducibility, deployment, contracts, aesthetics, semantics, security-drills, taxonomy, i18n, security-credential-registry, security-rotation, security-retention, security-auth, security-web]
