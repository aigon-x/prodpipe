# docs

> Katalog dokumentacji AIGON Production Platform — dokumentacja techniczna, architektura, polityki.

## 1. Purpose
Przechowuje dokumentację: techniczną, architektoniczną, polityki, procedury.

## 2. Owner
`STATUS: FOUNDATION PLACEHOLDER`

## 3. Source of Truth
Git jest źródłem prawdy (desired state) dla dokumentacji. Runtime odzwierciedla actual state.

## 4. Contains
Dokumentacja techniczna, architektoniczna, polityki, procedury, git policies.

## 5. Does Not Contain
Nie zawiera sekretów ani runtime state.

## 6. Dependencies
Zależy od governance, contracts, config.

## 7. Consumers
Operatorzy, architekci, deweloperzy.

## 8. Synchronization
Klasa: `CANONICAL` — dokumentacja jest źródłem prawdy w git.

## 9. Lifecycle
`STATUS: FOUNDATION PLACEHOLDER`

## 10. Security
Klasyfikacja danych: SYSTEM. Brak sekretów w git — tylko dokumentacja i referencje.

## 11. Recovery
`STATUS: FOUNDATION PLACEHOLDER` — procedury odzyskiwania dokumentacji.

## 12. Drift Detection
Wykrywanie rozjazdu między dokumentacją (git) a faktycznym stanem (Runtime). `STATUS: FOUNDATION PLACEHOLDER`.

## Examples
`STATUS: FOUNDATION PLACEHOLDER`
